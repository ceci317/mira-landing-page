# Mira Landing Page

Static landing page (single `index.html`) with local assets.

## Run locally

- Open `index.html` directly in a browser, or
- Serve with a local server:

```bash
python3 -m http.server 5173
```

Then visit `http://127.0.0.1:5173/`.

